<%= form_with(model: tarefa, local: true) do |form| %>
  <% if tarefa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tarefa.errors.count, "error") %> prohibited this tarefa from being saved:</h2>

      <ul>
      <% tarefa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descTarefa %>
    <%= form.text_field :descTarefa %>
  </div>

  <div class="field">
    <%= form.label :diaVencimento %>
    <%= form.number_field :diaVencimento %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
